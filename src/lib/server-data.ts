'use client';
/**
 * This file is now deprecated as we use client-side fetching via hooks.
 * We keep it as a placeholder to prevent import errors during transition.
 */
export const productsPromise = Promise.resolve([]);
export async function getProducts() { return []; }
export async function getCollections() { return []; }
